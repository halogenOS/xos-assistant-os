Changes from 5436fde95c93 to d995110e43d1:

2026-08-31 09:54  core: answer /compact with the operator's copy

The command answers "Compaction finished" — the operator's wording,
verbatim. The line confirms that the command took effect and explains
nothing; the mechanism lives in the group operator contract, which a
confirmation would only go stale repeating, as the unit 45 line did
when unit 48 changed what compaction does. Decision 0191 records the
choice and what it beat.

The ignored test demanding the answer describe the summarized half is
deleted rather than unignored: it recorded a copy gap for the operator
to settle, and the operator settled it the other way.

Test: fmt, clippy -D warnings, the workspace test suites, and doc with
-D warnings all exited 0 after the change.
---
2026-08-31 11:20  core: a failed turn is silent in the chat

A failed turn used to answer with one fixed line, and a payment-class
refusal was the one cause that passed without it. The line arrived
unsolicited when a restart cut a stream mid-turn, doubled under a torn
stream, and nothing about it was actionable in the chat.

Every failure now passes without a word: the stream-error arm delivers
what the dead turn already put on the ledger, writes one information
line naming the conversation and the error, and sends nothing. The
notice constant, the payment classification and the Notice reply kind
go; the latch and the addressed re-engagement stay exactly as they
were. Decision 0192 records the choice; 0025 and 0057 carry the
amendment note.

Test: fmt, clippy -D warnings, the workspace suites and doc -D warnings
all exited 0 after the change.
---
2026-08-31 11:20  Merge unit 49: a failed turn is silent in the chat

---
2026-08-31 11:21  assistant: version 0.4.2

The failed-turn silence ships as its own release: unit 49 plus the
/compact answer wording.

Test: cargo check --workspace exited 0 with the bumped version.
---
