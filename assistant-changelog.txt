Changes from b26edb80ecde to 88c616831263:

2026-09-06 01:34  tests: observe completed notes and retained retraction commands

The prompt-migration test accepted unfinished assistant output. The
refused-deletion test expected its command and the next message to share
one projected input, although interruption could separate them.

The migration fixture waits for the completed notes before shutdown. The
retraction fixture holds the closing round and observes its interruption,
then checks command retention and the following answer independently.

Add a live unfinished-output discriminator, check the retraction's delivery
and removed answer, and reuse the adapter's ledger wait for exact content.
Correct the deletion tests' acceptance-criterion references.

These observations distinguish stored completion from an open stream and
exercise deletion while the answer's closing round is still unfinished.
The tests no longer depend on incidental timing or message grouping, while
retaining the migration and retraction contracts. Product behavior is unchanged.

Test: cargo fmt --check (passed)
Test: cargo test -p assistant-core --test spine assembly:: (8 passed)
Test: cargo test -p assistant-adapter-telegram --test adapter retraction:: (4 passed)
Test: cargo test --workspace (949 passed, zero failed or ignored)
Test: cargo build --workspace (passed)
Test: git diff --check (passed)
---
2026-09-08 23:09  answering: teach the recent-message rule after resuming

The reply tool allowed answers to any message in the conversation, and the
answering instructions did not distinguish a resumed conversation from an
active exchange.

The instructions now exclude member messages more than ten member messages
back after unlatching or when the last successful group send is more than
twelve hours old. Reporting remains the exception under its existing rules.
Both sending tools follow this instruction, and older history stays available
as context. The reply description no longer grants unconditional permission
to answer an old member message.

This addresses answering an old backlog after a break. Composition tests
check both triggers and every answering mode without teaching unavailable
reporting tools. They verify the instruction, not model compliance.

Test: cargo test --workspace --all-features (950 tests passed)
Test: cargo clippy -p assistant-core --all-features --all-targets -- -D warnings passed
Test: rustfmt --check passed for the changed Rust files
---
